--extra-index-url https://download.pytorch.org/whl/cu121

# Exact versions verified end to end on a clean Windows / Python 3.11 machine:
# fresh venv -> these packages -> bridge imports, Whisper loads on CUDA, XTTS
# produces audio. No C++ compiler involved.
#
# INSTALL ORDER MATTERS — follow the step-by-step commands in the README and
# pass -c constraints.txt. torch must come from the cu121 index FIRST; if pip
# resolves a CPU torch instead, GPU inference dies silently.
# This file is the record of what the working environment contains.

# setuptools >= 81 dropped pkg_resources, which ctranslate2 imports at startup.
setuptools<81

torch==2.5.1+cu121
torchaudio==2.5.1+cu121

ntgcalls==2.1.0

Telethon==1.44.0
cryptg==0.6.0

# Pinned, not ranged: faster-whisper 1.2.x pulls ctranslate2 4.8, whose cuDNN
# clashes with this torch build ("Could not load symbol cudnnGetLibConfig").
faster-whisper==1.1.1
ctranslate2==4.6.0

# coqui-tts is the maintained fork of the archived TTS package. It ships a
# pure-Python wheel, so no C++ compiler is needed. TTS==0.22.0 has no wheel for
# Windows/Python 3.11 at all and forces every user to install MS Build Tools.
coqui-tts==0.27.5

# coqui-tts needs isin_mps_friendly (transformers >= 4.47); transformers 5.x
# removed it again. 4.57.6 is the version this was verified with.
transformers==4.57.6
tokenizers==0.22.2

# numpy must stay on 1.x — ctranslate2 breaks on 2.x.
numpy==1.26.4
scipy==1.17.1
soundfile==0.14.0
